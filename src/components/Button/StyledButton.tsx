const StyledButton = () => {
  return (
    <button className="styled_button py-1 px-5 rounded-lg bg-transparent font-bold border dark:border-white border-black">
      STYLED BUTTON
    </button>
  );
};

export default StyledButton;
